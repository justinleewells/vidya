import {{typeName}} from '../{{type}}'

const data = Object.freeze({
  id: {{id}},
  name: '{{name}}',
})

class {{className}} extends {{typeName}} {
  constructor(options) {
    super(data, options)
  }
}

export { data as default, {{className}} as Class }
